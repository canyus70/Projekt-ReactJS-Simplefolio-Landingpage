import "./NavBar.css"

const NavBar = () => {
    return (  

        <section className="wrappernav">
        <div>JS</div>
        <nav className="navbar">
            <a href="#">projects</a>
            <a href="#">skills</a>
            <a href="#">contact</a>
        </nav>
        </section>
    );
}

export default NavBar;